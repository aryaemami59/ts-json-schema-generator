import type { JSONSchema7 } from "json-schema";

/**
 * A JSON Schema draft-07 definition object used throughout the
 * generator's output. Aliased from {@linkcode JSONSchema7} so consumers retain
 * full type compatibility with the `json-schema` package.
 *
 * @see {@link https://json-schema.org/draft-07/json-schema-core | JSON Schema draft-07}
 */
export type Definition = JSONSchema7;
